/* 과를 입력하면 해당 과 진료 환자 수 반환 */
SELECT D.DEPTNAME, COUNT(P.NAME) AS PATIENT_COUNT
FROM DOCTOR D
LEFT JOIN PATIENT P ON P.DOC_ID = D.DOC_ID
GROUP BY D.DEPTNAME

/* 성별에 따른 환자 수 반환 */
SELECT P.SEX, COUNT(P.NAME) 
FROM PATIENT P
GROUP BY P.SEX
